name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run a one-line script
      run: git push origin master ssh://root@206.191.151.187/var/www/html/my-stuff

        
